# Fix plan

This project tracks all work in **beads (bd)**, not in this file. The worker
prompts (`.ralph/prompts/{opus,sonnet}.md`) tell ralph to call `bd ready` each
iteration and claim a bead.

The single unchecked item below is a deliberate placeholder so ralph's
fix_plan-based exit-condition check never fires. Ralph counts `- [ ]` items;
keep this one unchecked permanently. Don't add real tasks here — file beads
instead.

- [ ] Drive the bd queue (placeholder — do not check off; see `bd ready`)
