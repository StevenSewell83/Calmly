# Required
POSTGRES_PASSWORD=change-me-in-production

# Optional overrides (defaults shown)
POSTGRES_USER=calmly
POSTGRES_DB=calmly
PORT=3001

# Email — use "resend" in production with a real RESEND_API_KEY
EMAIL_SENDER=console
RESEND_API_KEY=

# Logging
LOG_LEVEL=info
