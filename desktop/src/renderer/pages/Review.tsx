import { PagePlaceholder } from "../components/PagePlaceholder";

export function Review() {
  return (
    <PagePlaceholder
      title="Review"
      subtitle="Daily shutdown — completed list, carry-forward, reflection."
      todo="CL-13 owns Review"
    />
  );
}
