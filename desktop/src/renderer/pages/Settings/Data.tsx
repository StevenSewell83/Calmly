import { PagePlaceholder } from "../../components/PagePlaceholder";

export function SettingsData() {
  return (
    <PagePlaceholder
      title="Data"
      subtitle="Export, sync status, and account deletion."
      todo="Epic 9 owns data export + telemetry opt-in"
    />
  );
}
