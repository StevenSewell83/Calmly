import { PagePlaceholder } from "../../components/PagePlaceholder";

export function SettingsReminders() {
  return (
    <PagePlaceholder
      title="Reminders"
      subtitle="Importance levels, escalation, channel preferences."
      todo="RM-01 owns reminder settings"
    />
  );
}
