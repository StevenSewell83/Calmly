import { PagePlaceholder } from "../../components/PagePlaceholder";

export function SettingsTelegram() {
  return (
    <PagePlaceholder
      title="Telegram"
      subtitle="Link your Telegram so capture works on the go."
      todo="TG-02 owns the linking flow"
    />
  );
}
