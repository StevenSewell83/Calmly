import { PagePlaceholder } from "../components/PagePlaceholder";

export function Inbox() {
  return (
    <PagePlaceholder
      title="Inbox"
      subtitle="Capture, browse, snooze, and triage. Real Inbox UI ships in Epic 2."
      todo="CL-03 owns the list view"
    />
  );
}
