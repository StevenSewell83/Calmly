import { PagePlaceholder } from "../components/PagePlaceholder";

export function Focus() {
  return (
    <PagePlaceholder
      title="Focus"
      subtitle="Current task + next step. Soft time, mark done, switch task."
      todo="CL-09 owns Focus mode"
    />
  );
}
