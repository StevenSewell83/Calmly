import { PagePlaceholder } from "../components/PagePlaceholder";

export function Plan() {
  return (
    <PagePlaceholder
      title="Plan"
      subtitle="A day grid with manual time blocks. Drag/drop reorder lands in Epic 2."
      todo="CL-06 owns the day grid"
    />
  );
}
