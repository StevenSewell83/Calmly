{
  "name": "@calmly/typecheck-baseline",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Gated typecheck — counts errors per (workspace, file, code), fails when new ones appear vs the baseline (calmly-d0s).",
  "scripts": {
    "check": "tsx check.ts",
    "snapshot": "tsx snapshot.ts",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "20.16.10",
    "tsx": "4.19.2",
    "typescript": "5.6.3",
    "vitest": "2.1.4"
  }
}
